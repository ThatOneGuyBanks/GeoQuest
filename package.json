{
  "name": "day-tripping-quiz",
  "version": "1.0.0-rc.1",
  "private": true,
  "type": "module",
  "scripts": {
    "check:backup": "node scripts/validate-backup.mjs",
    "test": "playwright test",
    "validate": "npm run check:backup && npm test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
